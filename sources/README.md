# Sources

- `seeds.json`: curated seed endpoints (California-first)
- `SEEDS.md`: human-readable documentation + usage guidance
- `fallback_matrix.md`: approved substitutes when a preferred endpoint is blocked/unavailable
- `certs/`: intermediate certificates used to stabilize fetches when servers omit the intermediate during TLS handshakes
